# AGENTS.md — features

- Uma pasta por domínio de UI; página em `page/index.tsx` (default export).
- Sem comentários.
- Incorporadora: wizard/editar/detalhe compartilham upload + viabilidade + progresso derivado.
- Admin: curadoria não inventa campos — lê o que a API devolve.
- Não criar rotas sem registrar em `src/router/index.tsx`.
