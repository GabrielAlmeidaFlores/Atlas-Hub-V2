# AGENTS.md — components

- `ui/` — primitivos reutilizáveis (botões via CSS global, DataTable, FilterBar).
- `shared/` — domínio Atlas (status badge, calculadora, progresso).
- Sem comentários.
- Preferir composição a props “god object”.
- Sem lógica de API aqui além de callbacks recebidos por props.
